# 6. Заключение

## Соответствие планируемым результатам практики

| № | Планируемый результат (из индивидуального плана практики) | Статус | Где реализовано |
|---|---|---|---|
| 1 | Обзор существующих платформ методов автопроверки | ✅ | [01-overview-platforms.md](01-overview-platforms.md) |
| 2 | Сравнение форматов заданий | ✅ | [02-task-formats-comparison.md](02-task-formats-comparison.md) |
| 3 | Генератор контента / seed-скрипты для наполнения курса | ✅ | `backend/app/seed/seed.py` + `theory_data.py`/`tasks_data.py` |
| 4 | Реализация модулей: теории, практики с проверкой, статистики | ✅ | `routers/theory.py`, `routers/tasks.py`, `routers/stats.py` + соответствующие страницы фронтенда |
| 5 | Скрипт автоматического тестирования платформы | ✅ | `backend/tests/` (58 pytest-тестов) + `frontend/tests/smoke.spec.js` (5 Playwright-тестов) + `scripts/run_all_tests.sh` |
| 6 | Демонстрация работы на провокационных примерах | ✅ | [04-edge-cases-demo.md](04-edge-cases-demo.md), `backend/tests/test_edge_cases.py` |
| 7 | Готовый курс из 6 теоретических блоков + 18 практических заданий + итоговый тест | ✅ (расширено) | 6 блоков теории, **30** практических заданий (5 на блок), **8** вопросов итогового теста |
| 8 | Анализ успешности: % верных ответов, время ответа | ✅ | [05-analytics.md](05-analytics.md), `GET /api/stats` |

Курс расширен за пределы минимальных требований задания: изначальные 18
практических заданий (по 3 на блок, форматы SINGLE/MULTIPLE_CHOICE,
FILL_BLANK, ORDERING, MATCHING, JSON_FIX) дополнены ещё 12 заданиями (по 2
на блок) — в том числе новым 6-м форматом **`API_REQUEST`**, где студент
пишет HTTP-запрос (метод, путь, при необходимости query-параметры и JSON-
тело) полностью самостоятельно, без выбора из готовых вариантов (см.
[02-task-formats-comparison.md](02-task-formats-comparison.md)). Итоговый
тест вырос до 8 вопросов; ни один из них не дублирует формулировки
практических заданий — только перенос знаний в новый контекст.

## Технологический стек

- Backend: Python 3.9, FastAPI, SQLAlchemy 2.0, Pydantic 2, SQLite, pytest
- Frontend: React 18, React Router 6, Vite 5, react-markdown, Playwright

## Ограничения текущей реализации (осознанные, для учебного масштаба)

- Аутентификация упрощена до анонимного `session_id` — нет регистрации,
  входа, восстановления доступа. Достаточно для демонстрации персонального
  прогресса, но не готово к реальному multi-device использованию одним
  пользователем (session_id привязан к конкретному браузеру/устройству
  через `localStorage`).
- Итоговый тест не ограничен по времени и допускает неограниченное число
  попыток — для реального экзамена потребовалась бы отдельная логика
  разовой/ограниченной по времени попытки.
- Метрика времени ответа считается на клиенте и не учитывает переключение
  вкладок/паузы пользователя.

## Возможные направления развития

- Полноценная аутентификация (email/пароль или OAuth2) вместо анонимных
  сессий — для сохранения прогресса между устройствами.
- Ограничение числа попыток и времени на итоговый тест.
- Экспорт прогресса/сертификата о прохождении курса в PDF.
- Админ-интерфейс для редактирования контента курса без правки Python-файлов
  напрямую (сейчас контент правится в `seed/theory_data.py`/`tasks_data.py`
  и заливается через `python -m app.seed.seed`).

## Сдача результатов

Код, методические материалы (`docs/`) и данный отчёт передаются
руководителю практики. Приложение развёрнуто на постоянном публичном
адресе для просмотра в любое время (без локального запуска):

**https://api-design-course-qyqg.onrender.com**

Код: [github.com/Dampazy/api-design-course](https://github.com/Dampazy/api-design-course).
Детали деплоя — [docs/DEPLOY.md](../DEPLOY.md). Для локального запуска —
[SETUP.md](../SETUP.md). Защита — демонстрация полного пользовательского
пути: теория → практика с автопроверкой → итоговый тест → страница
прогресса.
